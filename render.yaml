services:
  - type: web
    name: heroschool-agent
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEEPSEEK_API_KEY
        sync: false  # 需要在 Render 控制台中手动设置
